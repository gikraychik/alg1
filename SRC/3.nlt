# генерация контрольной суммы
# адрес возврата хранится в TMP0

INCLUDE(regs.inc)

IMOVE(0x3124, TMP1)
SUB(TMP1, T3, TMP1)
SUB(TMP1, T4, TMP1)
SUB(TMP1, CTRL_STATE, TMP1)
MOVE(TMP1, EE_SUM)

MOVE(TMP0, NETLIST_SELECT)